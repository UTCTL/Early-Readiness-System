<%= form_for(@exam_subject) do |f| %>
  <% if @exam_subject.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exam_subject.errors.count, "error") %> prohibited this exam_subject from being saved:</h2>

      <ul>
      <% @exam_subject.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
