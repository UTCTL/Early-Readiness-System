<%= form_for(@subtopic) do |f| %>
  <% if @subtopic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subtopic.errors.count, "error") %> prohibited this subtopic from being saved:</h2>

      <ul>
      <% @subtopic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :subject %><br />
     <%= select("subtopic", "subject_id", @subjects.collect {|s| [ s.name, s.id ] }) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
