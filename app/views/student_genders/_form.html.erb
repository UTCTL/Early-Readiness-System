<%= form_for(@student_gender) do |f| %>
  <% if @student_gender.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student_gender.errors.count, "error") %> prohibited this student_gender from being saved:</h2>

      <ul>
      <% @student_gender.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :student %><br />
    <%= f.text_field :student %>
  </div>
  <div class="field">
    <%= f.label :gender %><br />
    <%= f.text_field :gender %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
